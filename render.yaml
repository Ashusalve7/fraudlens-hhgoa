services:
  - type: web
    name: fraudlens-api
    runtime: python
    plan: free
    rootDir: fraudlens
    buildCommand: python -m pip install --upgrade uv && uv sync --all-groups --frozen
    startCommand: uv run uvicorn dashboard.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
      - key: TG_HOST
        sync: false
      - key: TG_SECRET
        sync: false
      - key: TG_GRAPHNAME
        value: FraudGraph
      - key: FRAUDLENS_ALLOWED_ORIGINS
        value: https://fraudlens-hhgoa-8ru.pages.dev
      - key: FRAUDLENS_API_TOKEN
        sync: false
